// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.test.suiswapexample.domain.buypool.hibernate;

import java.util.*;
import java.math.BigInteger;
import java.util.Date;
import org.test.suiswapexample.domain.*;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.test.suiswapexample.domain.buypool.*;
import org.test.suiswapexample.specialization.*;
import org.test.suiswapexample.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateBuyPoolStateRepository implements BuyPoolStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("Id", "Version", "X_Reserve", "X_Amounts", "X_TotalAmount", "Y_Reserve", "LiquidityTokenId", "X_BoughtAmount", "StartExchangeRateNumerator", "ExchangeRateNumerator", "ExchangeRateDenominator", "PriceCurveType", "PriceDeltaX_Amount", "PriceDeltaNumerator", "PriceDeltaDenominator", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted", "X_TokenType", "Y_TokenType"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public BuyPoolState get(String id, boolean nullAllowed) {
        BuyPoolState.SqlBuyPoolState state = (BuyPoolState.SqlBuyPoolState)getCurrentSession().get(AbstractBuyPoolState.SimpleBuyPoolState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractBuyPoolState.SimpleBuyPoolState();
            state.setId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (BuyPoolState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{BuyPoolState.SqlBuyPoolState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(BuyPoolState state) {
        BuyPoolState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (BuyPoolState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(BuyPoolState detached) {
        BuyPoolState persistent = getCurrentSession().get(AbstractBuyPoolState.SimpleBuyPoolState.class, detached.getId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().save(persistent);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(BuyPoolState persistent, BuyPoolState detached) {
        ((AbstractBuyPoolState) persistent).merge(detached);
    }

}

